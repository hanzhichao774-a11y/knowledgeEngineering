---
description: BizAgentOS 知识工程 Demo 项目工程纪律，在所有代码修改和提交时自动应用
globs:
alwaysApply: true
---

# 工程纪律规则

你正在协助开发 BizAgentOS 知识工程 Demo 项目。请严格遵守以下工程纪律。

## Git Commit 规范

生成 commit message 时必须使用 Conventional Commits 格式：

```
<type>(<scope>): <description>
```

- type 必须是：feat / fix / refactor / docs / style / chore / test
- scope 建议使用：frontend / backend / agent / graph / ws / docs
- description 用英文，小写开头，不加句号，描述"做了什么"
- 多文件改动时在 body 中分点列出关键变更

## 变更分级

每次改动前判断级别：

- S 级（新功能/架构变更）：必须更新 CHANGELOG.md + 相关文档
- A 级（Bug 修复/优化）：修复用户可感知 Bug 时更新 CHANGELOG.md
- B 级（文档/样式微调）：直接提交即可

## 文档同步检查清单

每次代码修改完成后，对照以下规则检查是否需要同步更新文档：

- 新增/修改/删除 REST API 端点或 WebSocket 事件 → 更新 `backend/API.md`
- 新增/修改/删除前端组件、Store 或 Hook → 更新 `frontend/COMPONENTS.md`
- 修改 TypeScript 核心类型定义（types/index.ts） → 检查 API.md 和 COMPONENTS.md
- 修改启动方式、构建流程或环境变量 → 更新 `docs/DEPLOY.md` + `README.md`
- 新增 npm 依赖 → 更新 `README.md` 技术栈表

如果有文档需要更新但未更新，必须主动提醒用户。

## 代码质量

- 修改前端代码后运行 `cd frontend && npm run lint` 确认无错误
- 修改后端代码后运行 `cd backend && npm run build` 确认编译通过
- 不要添加仅描述代码行为的冗余注释

## 项目结构

- 前端：`frontend/` — React + Vite + TypeScript + Zustand
- 后端：`backend/` — Fastify + TypeScript + WebSocket + Neo4j
- 文档：`README.md`、`CHANGELOG.md`、`backend/API.md`、`frontend/COMPONENTS.md`、`docs/DEPLOY.md`、`docs/ENGINEERING.md`
- KodaX 是独立仓库（已 gitignore），不要修改 `KodaX/` 下的文件
